# Copie para `.env` e preencha (o .env é gitignored — nunca commitar segredos).
# O analysis_utils carrega este .env automaticamente (procura em RKD/analysis/,
# RKD/ e na raiz do repo), então os notebooks acham a chave sem `export` manual.
WANDB_API_KEY=your_wandb_key_here      # de https://wandb.ai/authorize
WANDB_ENTITY=gabomfim-unicamp
WANDB_PROJECT=graph-rkd
