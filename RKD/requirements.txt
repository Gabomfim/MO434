# Dependências EXTRAS instaladas pelo container PyTorch do SageMaker (DLC já traz
# torch/torchvision). Auto-instalado quando presente na raiz do source_dir.
wandb==0.27.0
kagglehub>=1.0.1
scipy>=1.15
numpy<2.0
tqdm>=4.67
