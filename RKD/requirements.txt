# EXTRA dependencies installed by the SageMaker PyTorch container (the DLC already brings
# torch/torchvision). Auto-installed when present at the root of source_dir.
wandb==0.27.0
kagglehub>=1.0.1
scipy>=1.15
numpy<2.0
tqdm>=4.67
